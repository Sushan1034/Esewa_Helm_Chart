apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "esewa-testing.fullname" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "esewa-testing.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
